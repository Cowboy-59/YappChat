# Tasks

- [ ] T001 [high] Pre-flight: confirm no in-flight branches reference src/ or root app.py - Run `git branch --all` and grep for references to `src/`, root `app.py`, root `templates/index.html`. Block move if any active feature branch would be invalidated. Document findings in commit message before proceeding.
- [ ] T002 [high] git mv src/app.py → server/app.py and src/requirements.txt → server/requirements.txt - Use `git mv` (not copy+delete) to preserve git history for blame/log traceability. Verify `git log --follow server/app.py` shows the full history from the src/ drop.
- [ ] T003 [high] git mv src/templates/ → client/templates/ and seed client/static/{css,js,img}/.gitkeep - Move both index.html and login.html. Create empty static subdirectories with `.gitkeep` placeholders so they are tracked even when empty.
- [ ] T004 [medium] git mv src/setup_database.sql → db/migrations/001_init.sql - Rename to numbered migration filename for compatibility with future migration tooling (Alembic/sqitch/Flyway). Add a top-of-file comment noting it is idempotent via IF NOT EXISTS.
- [ ] T005 [low] git mv src/README.txt → docs/legacy-windows-deployment.txt - Preserve the Windows on-prem deployment instructions as a historical reference. Add a header noting it has been superseded by the Docker deployment in this spec.
- [ ] T006 [medium] Delete src/install_service.py and src/START_APP.bat - Windows-only artifacts (pywin32 service installer and batch launcher). Use `git rm` to record the removal. Both are explicitly replaced by Docker per the Spec 002 amendment OQ-18.
- [ ] T007 [low] Delete broken src/{templates,static directory - Empty nested directories created by a failed shell brace expansion during the original unzip. Use `git rm -r` rather than shell `rm -rf` to avoid Windows-shell fragility with the literal `{` in the path.
- [ ] T008 [high] Delete root-level v1 prototype: app.py, templates/, empty static/, empty data/ - Verified on 2026-05-13 that root app.py is a 196-line, 7-route, JSON-file-storage prototype superseded by the 902-line, 21-route src/app.py. Root templates/index.html is the 38KB v1 mockup, superseded by 57KB src/templates/index.html. Empty static/ and data/ are stubs that will be recreated by this spec in their proper locations.
- [ ] T009 [high] Edit server/app.py: env-var config + Flask template/static paths - Minimal edits only. (1) `Flask(__name__, template_folder='../client/templates', static_folder='../client/static')`. (2) Replace hardcoded DB_SERVER, DB_NAME, SERVER_IP, APP_PORT, SECRET_KEY at lines 10-19 with `os.environ.get(...)` reads. (3) Change connection string DRIVER to `{ODBC Driver 18 for SQL Server}` and add `Encrypt=yes;TrustServerCertificate=yes`. (4) UPLOAD_FOLDER honors env var, defaults to /data/uploads. No route logic changes.
- [ ] T010 [high] Write server/Dockerfile (python:3.11-slim + ODBC Driver 18) - Base: python:3.11-slim. Install msodbcsql18 + unixodbc-dev via Microsoft apt repo (pin GPG key handling). COPY server/. Install requirements.txt. EXPOSE 5000. CMD invokes entrypoint.sh which runs gunicorn -w 4 -b 0.0.0.0:5000 app:app. Document the known-fragile msodbcsql install snippet in a header comment.
- [ ] T011 [high] Write server/entrypoint.sh (db wait + init_db + gunicorn) - Bash script. Loop waiting for TCP connectivity to $DB_SERVER:1433 with a timeout. Once connected, execute a one-shot `python -c 'from app import init_db; init_db()'` to ensure schema is present. Then exec gunicorn. Chmod +x in Dockerfile.
- [ ] T012 [high] Write docker-compose.yml (app + db + named volumes) - Two services. `app`: build server/, depends_on db with healthcheck, env_file .env, volume `receipts:/data/uploads`, ports 5000:5000. `db`: image mcr.microsoft.com/mssql/server:2022-latest, env MSSQL_SA_PASSWORD + ACCEPT_EULA=Y, healthcheck via sqlcmd, volume `mssql-data:/var/opt/mssql`. Top-level `volumes:` block with receipts, mssql-data named.
- [ ] T013 [medium] Write .env.example with all required environment variables - Variables: DB_SERVER=db, DB_NAME=ExpenseManager, DB_USER=sa, DB_PASSWORD=ChangeMe_StrongPw123!, MSSQL_SA_PASSWORD must match DB_PASSWORD, SECRET_KEY=replace-me-with-32-bytes, UPLOAD_FOLDER=/data/uploads. Include comments noting which are required vs optional and the SQL Server SA password complexity requirements.
- [ ] T014 [medium] Add root .gitignore entries: data/, .env, __pycache__, *.pyc - Append to root .gitignore (do not overwrite existing entries). Verify data/.gitkeep is still tracked after the change.
- [ ] T015 [high] Smoke test: docker compose up --build + verify routes - On a clean clone: `cp .env.example .env`, edit passwords, `docker compose up --build`. Verify within 120s: /login returns 200, /api/periods returns [] with auth, init_db() created all 6 tables (check via sqlcmd in db container). Capture timing for Success Metric 1.
- [ ] T016 [high] Smoke test: receipt round-trip across compose restart - Upload a test receipt via POST /api/transactions/<tid>/receipt. Run `docker compose down && docker compose up`. Verify the receipt is still retrievable via GET /api/receipts/<filename>. Proves named volume `receipts` is correctly wired (Success Metric 3).
- [ ] T017 [medium] Final audit: no Windows-only code paths remain - grep -rE 'pywin32|win32serviceutil|C:\\Apps|\\\\' across server/ and client/. Should return zero hits. Also verify `git ls-files` shows no top-level app.py, templates/, empty static/, or empty data/ (Success Metric 4 and 5).
- [ ] T018 [low] Update root README.md with 'Run locally with Docker' section - Document: prerequisites (Docker Desktop 4.x, 4GB RAM allocated), quickstart (`cp .env.example .env` → `docker compose up --build`), how to reset the DB (`docker compose down -v`), where receipts persist (named volume `receipts`), and the known msodbcsql install fragility from Risk R1.