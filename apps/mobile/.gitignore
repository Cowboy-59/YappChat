# Expo / React Native
node_modules/
.expo/
dist/
web-build/
*.orig.*

# Native builds
/ios
/android

# Env / secrets
.env*.local

# FCM/APNs service-account PRIVATE keys — NEVER commit.
# (google-services.json is public client config and MAY be committed.)
*service-account*.json
fcm-*.json
google-service-account*.json
*-firebase-adminsdk-*.json
